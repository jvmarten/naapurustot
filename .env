# Map defaults (Helsinki metropolitan area)
VITE_MAP_CENTER_LNG=24.94
VITE_MAP_CENTER_LAT=60.17
VITE_MAP_ZOOM=9.2
VITE_MAP_MIN_ZOOM=2
VITE_MAP_MAX_ZOOM=16

# Basemap: OpenFreeMap vector styles (positron light / dark) — keyless, no watermark.
# The defaults live in src/utils/basemap.ts; uncomment to repoint at a self-hosted
# mirror (each is a full MapLibre style JSON URL).
# VITE_BASEMAP_STYLE_LIGHT_URL=https://tiles.openfreemap.org/styles/positron
# VITE_BASEMAP_STYLE_DARK_URL=https://tiles.openfreemap.org/styles/dark

# Cloudflare Turnstile site key — renders the bot-check widget on the signup form.
# This is the PUBLIC key (it ships in the client bundle by design); the matching
# SECRET key lives only in server/.env as TURNSTILE_SECRET. Empty → no widget (signup
# is then refused server-side in production, allowed in dev).
VITE_TURNSTILE_SITE_KEY=0x4AAAAAADsLrKCvXLli_3kX
